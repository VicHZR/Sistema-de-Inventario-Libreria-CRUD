info:
  name: Crear un autor
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}api/authors"
  body:
    type: json
    data: |-
      {
        "firstName": "Robert",
        "lastName": "Martin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
